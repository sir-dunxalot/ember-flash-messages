{{#each nonTimedMessages}}
  {{flash-message
    type=type
    content=content
    classPrefix=classPrefix
  }}
{{/each}}

{{#if currentMessage}}
  {{flash-message
    type=currentMessage.type
    content=currentMessage.content
    classPrefix=classPrefix
  }}
{{/if}}
