{{#each staticMessages}}
  {{flash-message
    action='removeMessage'
    classPrefix=classPrefix
    message=this
  }}
{{/each}}

{{#if currentMessage}}
  {{flash-message
    action='removeMessage'
    classPrefix=classPrefix
    message=currentMessage
  }}
{{/if}}
