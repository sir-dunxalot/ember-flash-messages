<dt {{bind-attr class='typeClass'}}>
  {{#if iconClass}}
    <span {{bind-attr class="iconClass"}} data-test="notify-icon"></span>
  {{/if}}

  <span data-test="notify-type">{{type}}</span>
</dt>

<dd {{bind-attr class='contentClass'}} data-test="notify-content">
  {{content}}
</dd>
